apply from: "${rootProject.projectDir}/library.gradle"
android {
    namespace 'com.benkkstudios.ads.startapp'
    buildFeatures {
        compose true
        viewBinding true
    }
}

dependencies {
    implementation(project(":shared"))
    implementation libs.inapp.sdk
}