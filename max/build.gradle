apply from: "${rootProject.projectDir}/library.gradle"
android {
    namespace 'com.benkkstudios.ads.max'
    buildFeatures {
        compose true
        viewBinding true
    }
}

dependencies {
    implementation(project(":shared"))
    implementation libs.applovin.sdk
}